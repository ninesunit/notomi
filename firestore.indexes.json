{
  "_comment": [
    "Notomi deliberately needs no composite indexes. Every query orders on a",
    "single field, which Firestore's automatic single-field indexes already",
    "serve; the open/overdue and per-subject splits are done client-side, where",
    "a student's data is small enough that it costs nothing.",
    "Keeping it this way means a fresh project works the moment rules are",
    "deployed, with no index build to wait on."
  ],
  "indexes": [],
  "fieldOverrides": [
    {
      "collectionGroup": "documents",
      "fieldPath": "text",
      "indexes": []
    }
  ]
}
